/**
 * `/componentes/novo` — criação de componente (página de composição).
 */

import { ComponenteFormPage } from './ComponenteFormPage'

export function ComponenteNovoPage() {
  return <ComponenteFormPage />
}
